server:
  port: 8080

spring:
  application:
    name: raven-webhook

logging:
  level:
    com.example.ravenwebhook: INFO
    org.springframework: WARN
  pattern:
    console: "%d{yyyy-MM-dd HH:mm:ss} - %msg%n"

management:
  endpoints:
    web:
      exposure:
        include: health,info
  endpoint:
    health:
      show-details: always
